// Integration tests will be added once the transport + provider layers are implemented.
// These tests require a running Anvil instance.

test "placeholder" {
    // Placeholder to allow build to succeed
}
